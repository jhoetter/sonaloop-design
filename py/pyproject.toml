[project]
name = "persona-icons"
version = "0.1.0"
description = "Shared persona icon library — Python SVG-string helpers (generated from icons.data.mjs)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["persona_icons"]
